<?xml version="1.0" encoding="utf-8"?>
<android.support.design.widget.CoordinatorLayout
    android:layout_height="match_parent"
    android:layout_width="match_parent"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <android.support.v7.widget.Toolbar
        android:id="@+id/toolbar_settlement"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:background="@color/white"
        app:popupTheme="@style/AppTheme.PopupOverlay"
        android:backgroundTint="@color/white">
    </android.support.v7.widget.Toolbar>
<RelativeLayout
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="cn.hwwwwh.lexiangdaxue.SelltementClass.activity.SettlementActivity"
    android:layout_marginTop="?attr/actionBarSize"
    android:layout_below="@+id/toolbar_settlement">
    <ScrollView
        android:id="@+id/settlement_Sl"
        android:layout_marginBottom="50dp"
        android:layout_width="match_parent"
        android:layout_height="match_parent">
        <include layout="@layout/settlement_detail"></include>
    </ScrollView>
    <RelativeLayout
        android:id="@+id/pay_view"
        android:layout_alignParentBottom="true"
        android:background="@color/white"
        android:layout_width="match_parent"
        android:layout_height="50dp">
        <TextView
            android:id="@+id/allValue"
            android:layout_marginLeft="15dp"
            android:textSize="20dp"
            android:layout_centerVertical="true"
            android:text="￥12"
            android:textColor="@color/orange"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content" />
        <Button
            android:id="@+id/pay"
            android:text="提交订单"
            android:textColor="@color/white"
            android:textSize="16sp"
            android:background="@color/orange"
            android:layout_alignParentRight="true"
            android:layout_width="100dp"
            android:layout_height="match_parent" />
    </RelativeLayout>
    <View
        android:id="@+id/fgx7"
        android:layout_width="match_parent"
        android:layout_above="@id/pay_view"
        android:layout_height="0.4dp"
        android:background="#cdcdcd" />
</RelativeLayout>
</android.support.design.widget.CoordinatorLayout>